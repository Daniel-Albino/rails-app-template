#!/usr/bin/env bash
# =============================================================================
# bin/docker-entrypoint
# Lightweight wrapper that delegates to the main Docker entrypoint.
# Some deploy providers (Render, Fly.io, Heroku) call this file.
# =============================================================================

set -e

exec /usr/local/bin/entrypoint.sh "$@"
